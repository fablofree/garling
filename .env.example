# Garage A. Lingiah — optional environment overrides
# These override the defaults in config/database.php and config/app.php.
# Not required for WampServer — you can edit the config files directly instead.

APP_DEBUG=true
APP_URL=http://localhost

DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=garage_lingiah
DB_USERNAME=root
DB_PASSWORD=
